{
  "name": "arammayhem",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "data": "node scripts/build-data.mjs",
    "assets": "node scripts/fetch-assets.mjs",
    "prepare-build": "npm run data && npm run assets",
    "dev": "npm run prepare-build && vite",
    "build": "npm run prepare-build && vite build",
    "preview": "vite preview",
    "capture": "node scripts/capture.mjs"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7"
  }
}